name: trend__ema__50
version: v1
family: trend
feature_type: rolling
owner: stock-engine
description: >
  EMA(50) of raw__close_adj__l1 on open sessions (min_periods=50).
dtype: float
unit: INR
dependencies:
  - feature:raw__close_adj__l1@v1
lookback_sessions: 50
pit_rule: asof_session_close
null_policy: propagate
lifecycle: experimental
validation:
  finite: true
  allow_null: true
recomputable: true
computational_cost: low
stability: experimental
